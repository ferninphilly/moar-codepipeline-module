version: 0.2

# aws codebuild start-build --project-name moar
phases:
  install:
    on-failure: ABORT
    commands:
      - yarn install
  build:
    commands:
      - ls
      - eslint ./src --quiet --ext .ts
